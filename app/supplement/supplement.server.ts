import mongoose from "mongoose";
import { Supplement, SupplementModel } from "./supplement.type";

export async function getSupplements(): Promise<Supplement[]> {
  let supplements: Supplement[];
  
  return supplements
}