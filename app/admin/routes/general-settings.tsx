/**
 * Allows admin to control general aspect of the site,
 * including the site name, email address,
 * membership and default user role, timezone and date/time format,
 * site default language
 *
 * @returns `jsx.Element`
 */
export default function GeneralSettings() {
  return <></>;
}
